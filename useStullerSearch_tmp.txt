  return {
    searchParams, setSearchParams,
    searchResults, loading, error,
    selectedProducts, setSelectedProducts,
    importing, importResults,
    tabValue, setTabValue,
    handleSearch, handleProductSelect,
    handleImport, handleClear
  };
}
